#ifndef BASE_HPP
#define BASE_HPP

class Base
{
    private:
        /* data */
    public:
        Base();
        Base(const Base& obj);
        Base& operator=(const Base& obj);
        ~Base();

        
};


#endif
